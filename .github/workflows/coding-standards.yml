name: coding standards

on:
  push:
    branches:
      - master
      - "*.x"
  pull_request:

permissions:
  contents: read

defaults:
  run:
    working-directory: src

jobs:
  fix:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.3
          tools: composer:v2
          coverage: none

      - name: Install dependencies
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 5
          max_attempts: 5
          command: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist --working-dir src

      - name: Execute coding standards verifications
        run: vendor/bin/pint --test -v
